export class Curtinha {
  id: number;
  titulo: string;
  resumo: string;
  link: string;

  constructor(id: number, titulo: string, resumo: string, link: string) {
      this.id = id;
      this.titulo = titulo;
      this.resumo = resumo;
      this.link = link;
  }
}
